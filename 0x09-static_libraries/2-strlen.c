#include "main.h"
#include <string.h>
/**
 * _strlen - determine length of string
 * @s: the string of length
 * Return: the length of a string passed
 */
int _strlen(char *s)
{
	int m;

	m = strlen(s);
	return (m);
}

