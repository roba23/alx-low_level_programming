#include "main.h"

/**
 * mul - accepts and multiplies two integers
 * and returns result
 * @a: 1st operand
 * @b:2nd operand
 * Return: return the result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
