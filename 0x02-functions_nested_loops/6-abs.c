#include "main.h"
#include <stdlib.h>
/**
 * _abs - find the absolute value of a number
 *
 * @a:accepted value
 * Return: returns the absolute value of a number
 */
int _abs(int a)
{
	int m = abs(a);

	return (m);

}
