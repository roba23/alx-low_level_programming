#include "main.h"
#include <stdio.h>
/**
 * add - takes two numbers and adds them together
 * @a: first operand
 * @b: second operand
 * Return: return the sum of the numbers
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}

